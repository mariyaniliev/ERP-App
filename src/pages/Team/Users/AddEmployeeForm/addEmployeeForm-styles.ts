import { THEME_SHADOW } from "../../../../theme/theme-constants";

export const styles = {
  shadow: {
    boxShadow: THEME_SHADOW,
  },
  formContainer: {
    display: "flex",
    flexDirection: "column",
  },
};
