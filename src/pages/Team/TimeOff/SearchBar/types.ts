export enum Period {
  future = "Future",
  today = "Today",
  past = "Past",
  period = "Period",
}

export enum TimeOffType {
  paid = "Paid",
  unpaid = "Unpaid",
  sickLeave = "Sick leave",
  type = "Type",
}
